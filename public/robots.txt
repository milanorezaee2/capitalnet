# ─── robots.txt — Capital Network (capitalnetwork.ir) ────────────────────────
# Generated by Bob — professional SEO configuration

User-agent: *
Allow: /

# ── Admin panel — never index, never follow ──────────────────────────────────
Disallow: /cp-secure
Disallow: /cp-secure/

# ── Compiled static assets — no crawl budget waste ───────────────────────────
Disallow: /assets/

# ── Tracking query parameters — prevent duplicate content indexing ────────────
# Google treats ?v= and ?link_id= as separate URLs; block them at crawl level.
Disallow: /*?v=
Disallow: /*?link_id=

# ── Sitemap location ──────────────────────────────────────────────────────────
Sitemap: https://capitalnetwork.ir/sitemap.xml
